trigger:
  ref:
    - refs/heads/master
    - refs/tags/*

kind: pipeline
type: exec
name: default

platform:
  os: darwin

steps:
  - name: yarn
    commands:
      - yarn
  - name: run on emu
    commands:
      - rnv run -p web -e next
